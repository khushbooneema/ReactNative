/**
 * @format
 */

import {AppRegistry} from 'react-native';
import {name as appName} from './app.json';
import { TabBar } from './Navigation/AppNavigation';

AppRegistry.registerComponent(appName, () => TabBar);
